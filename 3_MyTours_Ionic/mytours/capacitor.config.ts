import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.hsos.webanwendungen.mytours',
  appName: 'mytours',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
